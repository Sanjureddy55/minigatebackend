from django.apps import AppConfig


class SecurityGuardContactResidentConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name               = "apps.security_guard.contact_resident"
    label              = "security_guard_contact_resident"
    verbose_name       = "Security Guard — Contact Resident"
